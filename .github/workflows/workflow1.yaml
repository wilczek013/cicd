name: Test workflow
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          echo "Running tests..."
          echo "Tests complete"
      - uses: actions/setup-node@v3
        with:
          node-version: "18.x"
      - name: Run app
        run: npm start
      - name: Check tools
        run: |
          echo "Checking tools..."
          curl --version
          echo "Tools check complete"
